-- Synthetic fixtures only. The caller wraps this file in BEGIN / ROLLBACK.
do $$
declare owner_user uuid:=gen_random_uuid(); homeowner uuid:=gen_random_uuid(); outsider uuid:=gen_random_uuid();
 company uuid; other_company uuid; call jsonb; repeat_call jsonb; context jsonb; invitation_code text;
 intake uuid; first_home uuid; receipt jsonb; retry_receipt jsonb; setup_call jsonb;
 email text:='intake-'||homeowner::text||'@example.invalid'; denied boolean;
 home_input jsonb:=jsonb_build_object('p_name','Intake test home','p_address_line_1','100 Test Street','p_city','Riverside','p_state','CA','p_postal_code','92501','p_country_code','US','p_formatted_address','100 Test Street, Riverside, CA 92501','p_latitude',33.98,'p_longitude',-117.37,'p_google_place_id','intake-test-'||homeowner::text,'p_property_type','HOUSE');
begin
 insert into auth.users(id,email,email_confirmed_at) values(owner_user,'owner-'||owner_user::text||'@example.invalid',now()),(homeowner,email,now()),(outsider,'outsider-'||outsider::text||'@example.invalid',now());
 insert into public.profiles(id,email,role) values(homeowner,email,'HOMEOWNER') on conflict(id) do update set role='HOMEOWNER';
 insert into public.companies(name,status,service_categories) values('Intake test company - rolled back','active',array['Plumbing']) returning id into company;
 insert into public.companies(name,status,service_categories) values('Other intake company - rolled back','active',array['Plumbing']) returning id into other_company;
 insert into public.company_users(company_id,auth_user_id,full_name,role,status) values(company,owner_user,'Intake test owner','owner','active'),(other_company,outsider,'Other owner','owner','active');
 perform set_config('request.jwt.claim.sub',owner_user::text,true);
 perform set_config('request.jwt.claims',jsonb_build_object('sub',owner_user,'email','owner@example.invalid','role','authenticated')::text,true);
 execute 'set local role authenticated';
 call:=public.create_company_customer_intake(company,'retry-test-key',jsonb_build_object('invitedName','Test homeowner','invitedEmail',email,'invitedPhone','5550100100','note','PRIVATE OFFICE NOTE','customerKind','existing','serviceReason','warranty','urgency','emergency','customerSummary','Possible leak','addressHint','Test address','previousWorkReference','Previous test work'));
 repeat_call:=public.create_company_customer_intake(company,'retry-test-key','{}');
 if call->>'invitation_id' is distinct from repeat_call->>'invitation_id' then raise exception 'Retry created another invitation'; end if;
 intake:=(call->>'intake_id')::uuid;
 if jsonb_array_length(public.get_company_customer_intakes(company))<>1 then raise exception 'Dispatch cannot see unopened call'; end if;
 execute 'reset role';
 update public.company_invitation_rate_events set created_at=now()-interval '2 hours' where company_id=company;
 execute 'set local role authenticated';
 setup_call:=public.create_company_customer_intake(company,'setup-only-key',jsonb_build_object('invitedName','Setup customer','invitedEmail',email,'invitedPhone','5550100100','customerKind','existing','serviceReason','setup','urgency','emergency'));
 if jsonb_array_length(public.get_company_customer_intakes(company))<>1 then raise exception 'Setup invitation created a dispatch call'; end if;
 execute 'reset role';
 select invite_code into invitation_code from public.company_customer_invitations where id=(call->>'invitation_id')::uuid;
 if exists(select 1 from public.service_requests where company_id=company) then raise exception 'A fake request was created without a home'; end if;
 if exists(select 1 from public.get_customer_invite_by_code(invitation_code) where note is not null) then raise exception 'Internal office note leaked through invitation'; end if;

 perform set_config('request.jwt.claim.sub',outsider::text,true);
 perform set_config('request.jwt.claims',jsonb_build_object('sub',outsider,'email','outsider@example.invalid','role','authenticated')::text,true);
 execute 'set local role authenticated';
 denied:=false; begin perform public.get_my_customer_intake(invitation_code,null); exception when others then denied:=true; end;
 if not denied then raise exception 'Another customer read private intake'; end if;
 denied:=false; begin perform public.get_company_customer_intakes(company); exception when others then denied:=true; end;
 if not denied then raise exception 'Another company read intake'; end if;
 if exists(select 1 from public.company_customer_intakes where company_id=company) then raise exception 'Cross-company table read bypassed RLS'; end if;
 execute 'reset role';

 perform set_config('request.jwt.claim.sub',homeowner::text,true);
 perform set_config('request.jwt.claims',jsonb_build_object('sub',homeowner,'email',email,'role','authenticated')::text,true);
 execute 'set local role authenticated';
 context:=public.get_my_customer_intake(invitation_code,null);
 if context::text like '%PRIVATE OFFICE NOTE%' or context ? 'creation_key' then raise exception 'Customer response contains office-only data'; end if;
 if exists(select 1 from public.company_customer_intakes where company_id=company) then raise exception 'Homeowner can read raw office intake table'; end if;
 denied:=false; begin perform public.submit_my_customer_intake(intake,'leak','regular',null); exception when others then denied:=true; end;
 if not denied then raise exception 'Request submitted without contact/address'; end if;
 perform public.save_my_customer_intake(intake,jsonb_build_object('name','Test homeowner','email','corrected@example.invalid','phone','5550100200'),null,null);
 first_home:=public.create_my_customer_intake_home(intake,home_input);
 if public.create_my_customer_intake_home(intake,home_input)<>first_home then raise exception 'Retry duplicated home'; end if;
 perform public.save_my_customer_intake(intake,null,first_home,jsonb_build_object('issue','Leak at fixture','accessInstructions','private test access'));
 receipt:=public.submit_my_customer_intake(intake,'Leak at fixture','regular','private test access');
 retry_receipt:=public.submit_my_customer_intake(intake,'Second tap','emergency',null);
 if receipt->>'service_request_id' is distinct from retry_receipt->>'service_request_id' then raise exception 'Retry duplicated service request'; end if;
 if receipt->>'request_type'<>'emergency' then raise exception 'Office emergency was downgraded'; end if;
 execute 'reset role';
 if (select count(*) from public.service_requests where company_id=company)<>1 then raise exception 'Expected exactly one request'; end if;
 if not exists(select 1 from public.service_requests where id=(receipt->>'service_request_id')::uuid and issue_summary like 'Warranty review requested:%') then raise exception 'Warranty reason lost'; end if;
 if not exists(select 1 from public.property_preferred_providers where company_id=company and property_id=first_home and status='active') then raise exception 'Inviting company not preferred'; end if;
 if (select count(*) from public.properties where owner_id=homeowner)<>1 then raise exception 'Duplicate homeowner property'; end if;
 perform set_config('request.jwt.claim.sub',owner_user::text,true);
 execute 'set local role authenticated';
 if jsonb_array_length(public.get_company_customer_intakes(company))<>0 then raise exception 'Submitted call still duplicated in pending queue'; end if;
 execute 'reset role';
 -- Existing HomeOS customers reuse their home for maintenance, without duplicating identity.
 update public.company_invitation_rate_events set created_at=now()-interval '2 hours' where company_id=company;
 execute 'set local role authenticated';
 call:=public.create_company_customer_intake(company,'maintenance-key',jsonb_build_object('invitedName','Test homeowner','invitedEmail',email,'invitedPhone','5550100100','customerKind','existing','serviceReason','maintenance','urgency','regular','knownPropertyId',first_home));
 execute 'reset role';
 intake:=(call->>'intake_id')::uuid;
 perform set_config('request.jwt.claim.sub',homeowner::text,true);
 perform set_config('request.jwt.claims',jsonb_build_object('sub',homeowner,'email',email,'role','authenticated')::text,true);
 execute 'set local role authenticated';
 perform public.save_my_customer_intake(intake,jsonb_build_object('name','Test homeowner','email',email,'phone','5550100100'),first_home,null);
 receipt:=public.submit_my_customer_intake(intake,'Annual maintenance','regular',null);
 perform public.finish_my_customer_intake(intake);
 context:=public.get_my_customer_intake(null,intake);
 if context->>'media_completed_at' is null then raise exception 'Completed intake cannot be resumed as confirmation'; end if;
 denied:=false; begin perform public.submit_my_customer_intake((setup_call->>'intake_id')::uuid,'Should not submit','regular',null); exception when others then denied:=true; end;
 if not denied then raise exception 'Setup-only invitation allowed a service request'; end if;
 execute 'reset role';
 if not exists(select 1 from public.service_requests where id=(receipt->>'service_request_id')::uuid and request_type='regular' and issue_summary like 'Maintenance request:%') then raise exception 'Maintenance reason or queue lost'; end if;
 if (select count(*) from public.properties where owner_id=homeowner)<>1 then raise exception 'Existing customer got duplicate home'; end if;
 -- Closing or revoking an invitation prevents customer intake writes.
 perform set_config('request.jwt.claim.sub',owner_user::text,true);
 execute 'set local role authenticated';
 perform public.cancel_company_customer_intake((setup_call->>'intake_id')::uuid);
 execute 'reset role';
 perform set_config('request.jwt.claim.sub',homeowner::text,true);
 execute 'set local role authenticated';
 denied:=false; begin perform public.get_my_customer_intake(null,(setup_call->>'intake_id')::uuid); exception when others then denied:=true; end;
 if not denied then raise exception 'Cancelled intake can be accessed'; end if;
 execute 'reset role';
 update public.company_customer_invitations set revoked_at=now(),status='revoked' where id=(call->>'invitation_id')::uuid;
 execute 'set local role authenticated';
 denied:=false; begin perform public.save_my_customer_intake(intake,null,null,'{}'); exception when others then denied:=true; end;
 if not denied then raise exception 'Revoked invitation still allows changes'; end if;
 execute 'reset role';
 if has_function_privilege('anon','public.get_my_customer_intake(text,uuid)','execute') or has_function_privilege('anon','public.submit_my_customer_intake(uuid,text,text,text)','execute') then raise exception 'Anonymous user has private intake access'; end if;
end $$;
select 'PASS: office intake, setup-only, idempotent invite/home/request, emergency warranty, preferred company and cross-company/customer privacy' as result;
